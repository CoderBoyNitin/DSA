#include<iostream>

using namespace std;

int main(){
    cout << "enter the value of x and y : ";
    double x;
    double y;
    cin >> x; // cin >> x >> y;
    cin >> y;
    cout <<"the value of x + y is :" << (x + y);
}